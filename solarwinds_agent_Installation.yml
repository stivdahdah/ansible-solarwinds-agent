---
- name: Installing solawrWinds Agent on Linux Devices
  hosts: all
  become: true
  tasks:
    - name: Using the 1 line command to deploy solarwind agents
      shell: ([ "AUTO_SHEL" = "" ] ...... ')
      register: output

    - debug:
        var: output
